# Anything not needed inside the API build context.
# Smaller context = faster uploads to the Docker daemon and better cache hits.

node_modules
dist
.next

# Sibling packages — built in their own contexts.
web
mcp

# Generated prisma client — rebuilt inside the image.
src/generated/prisma

# Git + CI metadata
.git
.github
.gitignore

# Env files — never bake secrets into an image. Coolify injects at runtime.
.env
.env.*
!.env.example

# Local tooling
tests
coverage
*.log
.DS_Store
.vscode
.idea

# Docs / architecture — not needed at runtime
Architecture
README.md
docker-compose*.yml
Dockerfile
scripts
# …but db-seed.js is the wrapper invoked by `npm run db:seed` inside the
# prod container, so it has to ship in the image. Re-include it.
!scripts/db-seed.js
